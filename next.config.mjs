/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['aceternity.com',
                "images.unsplash.com",

      ], // Add the hostname here
    },
  };
  
  export default nextConfig;
  